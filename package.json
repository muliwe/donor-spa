{
  "name": "donor-spa",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node ./server/server",
    "pretest": "eslint --quiet .",
    "test": "NODE_ENV=test mocha server/tests/socket/ --reporter spec"
  },
  "dependencies": {
    "express": "^4.15.4",
    "mongoose": "^4.11.8",
    "socket.io": "^2.0.3"
  },
  "devDependencies": {
    "chai": "^4.1.1",
    "eslint": "^4.5.0",
    "eslint-config-google": "^0.9.1",
    "eslint-plugin-tdd": "^0.5.0",
    "mocha": "^3.5.0",
    "socket-tester": "^1.2.1",
    "socket.io-client": "^2.0.3"
  }
}
